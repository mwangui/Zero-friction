{
  "name": "pulsefabric-monitor",
  "version": "0.0.0",
  "private": true,
  "description": "PulseFabric Monitor — GitHub Codespaces + Copilot Demo",
  "scripts": {
    "dev:web": "cd frontend && npx ng serve",
    "build:web": "cd frontend && npx ng build",
    "dev:api": "npx json-server backend/db.json --port 3000",
    "dev": "npx concurrently -n api,web -c blue,green \"npm run dev:api\" \"npm run dev:web\"",
    "validate": "cd frontend && npx ng build && npx ng lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "json-server": "^1.0.0-beta.15"
  }
}
